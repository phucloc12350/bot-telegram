import { GoldDashboard } from './gold-dashboard';

export const metadata = { title: 'Thống kê vàng' };
export const dynamic = 'force-dynamic';

export default function GoldPage() {
  return <GoldDashboard />;
}
